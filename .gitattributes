# Normalize line endings. Shell scripts and Python must stay LF everywhere,
# otherwise a CRLF checkout breaks the `#!` shebang on Linux/macOS.
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
